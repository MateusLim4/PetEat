name: peteat
description: A new Flutter project.

publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  sqflite: ^2.0.0+3
  cupertino_icons: ^1.0.2
  firebase_core: ^1.7.0
  google_fonts: ^2.1.0
  google_sign_in: ^5.1.1
  shared_preferences: ^2.0.8
  fl_chart: ^0.40.0
  weekday_selector: ^1.0.0
  flutter_local_notifications: ^9.0.1
  flutter_staggered_grid_view: ^0.4.1
  flutter_launcher_icons: ^0.9.2

flutter_icons:
  image_path: "assets/images/pawprintPink.png"
  android: true
  ios: true

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/