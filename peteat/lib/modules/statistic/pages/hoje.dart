import 'package:flutter/material.dart';

class TodayChart extends StatelessWidget {
  const TodayChart({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('Gráficos de Hoje'));
  }
}
